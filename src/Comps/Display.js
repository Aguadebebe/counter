

const Display = ({ counterValue }) => {
    return (

        <div>
          <p> Counter: { counterValue } </p>
        </div>
    )
}

export default Display;

/** Displays the counterValue on the screen */